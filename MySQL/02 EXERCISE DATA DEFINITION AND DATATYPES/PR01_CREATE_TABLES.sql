CREATE TABLE `minions` (
`id` INT primary key auto_increment ,
`name` varchar(50) not null,
`age` int
);

CREATE TABLE `towns` (
`town_id` int primary key auto_increment,
`name` varchar(30) not nullminions
);